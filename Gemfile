# Gemfile
source "https://rubygems.org"

gem "jekyll"
# Use the official GitHub Pages gem to ensure compatibility with GitHub's build process, 
# even though we are deploying on Netlify.
gem "github-pages", group: :jekyll_plugins
